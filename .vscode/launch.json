{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug voxelcraft.web (Chrome)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://127.0.0.1:5173/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "web: dev (vite)"
    }
  ]
}

